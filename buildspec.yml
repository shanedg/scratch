version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
      - npm install
      # - npm install -g node webpack
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      - npm run install
      # - command
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
    - dist/server.js
    # - location
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
